package com.franz.agbase;

/**
 * This interface includes all the types that can be returned as the result of a query.
 */
public interface ValueObject {

	public int compareTo(Object to);

}